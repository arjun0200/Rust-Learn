{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust Tests",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": ["test", "--no-run"]
            },
            "program": "${cargo:test}",
            "args": [],
            "cwd": "${workspaceFolder}/LinkedList",
            "stopOnEntry": false,
            "terminal": "integrated"
        }
    ]
}
